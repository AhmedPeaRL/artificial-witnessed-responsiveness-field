export const TemporalWitnessConfig = {
  bufferSize: 1200,          // عدد العينات القصوى
  injectionDelay: 9000,      // تأخير زمني بالمللي ثانية
  decayFactor: 0.0004,       // معدل تلاشي الأثر
  influenceRadius: 180,      // مدى التأثير عند الحقن
  silent: true               // لا رسائل – لا لوج
};
